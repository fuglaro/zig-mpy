//////////
// Header file for micropython modules and the zig library.
// Only defines functions in root.zig that should be called
// from micropython.
//

int factorial_helper(int);
int add(int, int);
void __init__();
int increment();
